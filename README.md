# Masking Remover - 수업용 가림판 및 순차 공개 도구

> **수업 교재(PDF, 이미지)의 문제 정답, 핵심 키워드, 해설 부분을 드래그로 가리고 순서에 따라 차례대로 지워주는 웹 기반 수업 도구입니다.**

---

## 🌟 주요 기능

1. **문서 및 이미지 완벽 지원**
   - **PDF 파일 업로드**: `PDF.js` 엔진을 통해 다중 페이지 문서를 선명하게 렌더링하고 페이지별 마스킹을 각각 보존합니다.
   - **이미지 업로드**: PNG, JPG, WEBP, GIF 등 지원.
   - **드래그 앤 드롭**: 브라우저 창 위에 파일을 끌어다 놓으면 즉시 열립니다.
   - **샘플 교재 기본 탑재**: 실행하자마자 바로 사용법을 체험할 수 있는 인터랙티브 예시 학습지가 제공됩니다.

2. **직관적인 가림판(마스킹) 생성, 텍스트 입력 및 드래그 앤 드롭 이동**
   - 마우스나 스마트패드/전자칠판 터치 드래그로 원하는 영역에 사각형 가림판을 쉽게 생성합니다.
   - 가림판 생성 순서에 따라 **①, ②, ③...** 순번 배지가 자동으로 부여됩니다.
   - **이미 생성된 마커 드래그 앤 드롭 이동**: 이미 배치된 가림판 박스를 마우스로 끌어다 놓아 자유롭게 위치를 이동할 수 있습니다 (상대 좌표계 % 자동 갱신).
   - **가림판 텍스트 입력**: 가림판을 **더블 클릭**하거나 사이드바 목록에서 힌트/단어/정답 텍스트를 직접 입력할 수 있습니다.
   - 창 크기 변경이나 확대/축소(Zoom)에도 가림판 위치가 정확히 유지되는 **상대 좌표계(%)** 기반 설계.

3. **대형 단어 카드 포커스 모드 (Flashcard Mode)**
   - 가림판의 힌트/단어와 가려진 실제 영역을 화면 중앙에 **기존 2배 크기의 대형 플래시 카드** 형식으로 띄워 집중 퀴즈를 진행합니다 (교실 맨 뒤에서도 선명한 가독성).
   - **단어 카드 상태 단축키 연동**: 단어 카드 포커스 상태에서도 `Space` 또는 `→` 키를 누르면 다음 가림판의 단어 카드로 자연스럽게 넘어가며 순차 학습이 가능합니다.
   - 클릭이나 `Enter` 키로 가림 상태와 정답 공개 상태를 즉시 토글할 수 있습니다.
   - 교육적인 플래시 카드 SVG 아이콘 적용.

4. **수업용 가림판 스타일 테마**
   - **칠판 다크 (Slate Black)**: 눈이 편안한 수업용 다크 마스크
   - **포스트잇 (Sticky Yellow)**: 화사하고 주목도 높은 메모형 마스크
   - **클래스 블루 (Modern Blue)**: 깔끔한 강조용 블루 마스크
   - **교사용 힌트 (Blur Mode)**: 교사에게는 흐릿하게 비쳐 보이고 학생에게는 가려지는 반투명 블러 마스크

5. **스마트한 순차 공개 (지우기) 엔진**
   - **단축키 제어**: `Space` 또는 `→` 키를 누르면 1번부터 차례대로 스무스한 페이드 아웃/블러 트랜지션과 함께 가림판이 지워집니다.
   - **이전 복구**: 실수로 지웠을 때 `←` 키로 방금 지운 가림판을 즉시 복원합니다.
   - **직접 클릭 공개**: 특정 번호의 가림판을 마우스로 클릭하면 순서와 관계없이 해당 가림판만 먼저 열 수 있습니다.
   - **일괄 제어**: 모든 가림판 다시 가리기(`R`), 모든 가림판 한 번에 공개(`A`).
   - **오디오 피드백**: 가림판이 지워지고 복구될 때 부드러운 사운드 피드백 제공 (Web Audio API).

6. **수업 준비 데이터 및 템플릿(프리셋) 보관함**
   - **교재 무관 프리셋 저장**: 특정 교재 파일과 독립적으로 마스킹 형태와 텍스트를 이름별로 브라우저 로컬 저장소에 저장하고 필요할 때 원클릭으로 다시 불러올 수 있습니다.
   - **JSON 파일 보관**: 교무실이나 집에서 구성한 가림판 설정을 `.json` 파일로 내보내거나 불러올 수 있습니다.

7. **수업 프레젠테이션 최적화 편의 기능**
   - **초슬림 플로팅 진행상황 패널 & 위치 드래그**: 패널 좌측 그립 핸들을 끌어다 화면 원하는 곳으로 배치할 수 있으며, 높이 최소화 및 텍스트 줄바꿈 방지로 수업 자료를 가리지 않습니다.
   - **수업 진행(전체) 모드 사이드바 자동 숨김**: 수업에 집중할 수 있도록 전체 모드 진입 시 사이드바가 자동으로 닫힙니다.
   - **전체화면 모드 시 자동 가로 맞춤**: 전체화면(`F` / `F11`) 진입 시 항상 화면 너비 맞춤(Fit Width)이 기본으로 자동 적용되어 빔프로젝터/전자칠판에 꽉 찬 뷰를 제공합니다.
   - **Ctrl + 마우스 휠 줌**: `Ctrl` 키를 누른 채 마우스 휠을 굴려 원하는 배율로 부드럽게 확대/축소할 수 있습니다.
   - **사이드바 하단 단축키 가이드**: 왼쪽 패널 하단에 주요 단축키 안내 카드가 상시 표시됩니다.

---

## ⌨️ 단축키 안내

| 키 (Key) | 기능 설명 |
| :--- | :--- |
| **`Space`** 또는 **`→`** | 다음 순번 가림판 지우기 / 단어 카드 포커스 시 다음 카드로 이동 |
| **`←`** | 방금 지운 가림판 복구 / 단어 카드 포커스 시 이전 카드로 이동 |
| **`Ctrl + Z`** | **실행 취소 / 되돌리기** (가림판 공개 복구, 생성/삭제 취소, 전체 공개/가리기 복구) |
| **`C`** | 단어 카드 포커스 모드 열기 / 닫기 |
| **`Enter`** | 단어 카드 포커스 모드에서 카드 내용(정답) 공개 / 가림 토글 |
| **`Ctrl + P`** | 학생용 유인물 인쇄 / PDF 저장 대화상자 열기 |
| **`Ctrl + 휠`** | 뷰포트 확대 / 축소 (Zoom In / Out) |
| **`R`** | 모든 가림판 다시 가리기 (처음 상태로 리셋) |
| **`A`** | 모든 가림판 한 번에 전체 공개 |
| **마커 마우스 드래그** | 이미 생성된 가림판 박스를 원하는 위치로 이동 |
| **가림판 더블클릭** | 가림판에 힌트/단어 텍스트 직접 입력 및 편집 |
| **가림판 직접 클릭** | 순서와 상관없이 해당 번호의 가림판 즉시 공개/가리기 |
| **진행 패널 핸들 드래그** | 하단 플로팅 진행 바를 화면 원하는 위치로 이동 |
| **`F`** / **`F11`** | 전체화면 모드 토글 (자동으로 화면 가로 너비 맞춤 적용) |
| **`PageUp` / `PageDown`** | PDF 다중 페이지 문서 이전 / 다음 페이지 이동 |

---

## 🚀 실행 방법

별도의 서버 설치나 빌드 과정이 필요 없습니다.

1. `06masking_remover` 폴더 내의 [`index.html`](file:///c:/Users/user/Desktop/local/06masking_remover/index.html) 파일을 크롬(Chrome), 엣지(Edge), 웨일(Whale) 등 최신 웹 브라우저에서 **더블 클릭**하여 실행합니다.
2. 실행 즉시 나타나는 예제 화면에서 `Space` 바를 눌러 가림판이 순서대로 지워지는 것을 확인해보세요!
3. 상단 **[파일 열기]** 버튼 또는 브라우저 창으로 원하는 PDF나 이미지를 끌어다 놓아 수업에 활용하시면 됩니다.

---

## [2026-09-17 08:52] 업데이트 이력 (Commit ID: 3540c8f64030f74ab1b6050f411ea88527535733)
- **수정 내용**: 수업용 가림판 및 순차 공개 웹 도구(EduMask) 전체 기능 구현
  - PDF.js 기반 다중 페이지 PDF 렌더링 및 이미지 로딩 지원
  - 마우스/터치 드래그 기반 반응형 사각형 가림판 생성 (순번 자동 부여)
  - 4가지 수업용 테마(칠판 다크, 포스트잇, 클래스 블루, 교사용 힌트 블러) 지원
  - Space/화살표 단축키 및 클릭을 통한 순차 공개(지우기) 엔진 및 복구 로직 구현
  - 수업 준비 상태 JSON 내보내기/불러오기 기능 구현
  - 즉시 체험 가능한 샘플 학습지 캔버스 기본 제공
- **검증 결과**:
  - `node --check app.js` 구문 분석 검증 성공 (0 에러)
  - `index.html`과 `app.js` 간 38개 DOM 엘리먼트 ID 상호 참조 정합성 검증 완료
  - 로컬 `file://` 환경에서 외부 서버 없이 독립 구동 검증 완료

## [2026-09-17 09:14] 업데이트 이력 (Commit ID: 528e110d8d689d79d1376f5d226c9e3f6c9946d1)
- **수정 내용**:
  - **이미 생성된 마커(가림판) 마우스 드래그 앤 드롭 이동**: 마스크 박스를 마우스로 드래그하여 문서 상의 원하는 위치로 자유롭게 이동 및 상대 좌표(%) 실시간 자동 재계산 기능 구현 (클릭/더블클릭 동작과 완벽 분리)
  - **단어 카드 포커스 창 2배 대형화**: 교실 맨 뒤 학생까지 선명하게 보이도록 모달 최대 너비 1200px 확장 및 가변 폰트 대형화, 교육적인 플래시 카드 SVG 아이콘 적용
  - **단어 카드 포커스 단축키 연속 탐색**: 단어 카드 열림 상태에서 `Space` 또는 방향키로 다음/이전 단어 카드로 즉시 전환 기능 지원
  - **초슬림 플로팅 진행상황 패널 & 드래그 앤 드롭 이동**: 좌측 드래그 핸들을 통해 화면 어디로든 이동 가능, 세로 높이 최소화 및 버튼 텍스트 줄바꿈 방지 스타일 적용
  - **수업 진행(전체) 모드 사이드바 자동 숨김**: 방해 없는 수업 환경을 위해 전체 모드 시 사이드바 자동 축소
  - **교재 독립 마스킹 템플릿(프리셋) 보관함**: 교재 파일과 별개로 마스킹 배치를 이름별로 로컬 스토리지에 저장하고 재사용 가능한 관리 시스템 구현
  - **사이드바 하단 단축키 가이드 상시 표시**: 수업 중 언제든 단축키를 참고할 수 있도록 왼쪽 패널 하단에 단축키 표 배치
  - **전체화면 시 화면 가로 맞춤 기본 적용 및 Ctrl + 마우스 휠 줌 기능 구현**
  - **프로젝트 명칭 'Masking Remover' 공식 적용**
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - `verify_ids.js` 정적 분석을 통해 `index.html`과 `app.js` 간 57개 DOM 엘리먼트 ID 전수 일치 검증 완료 (누락 ID 0개)
  - 마커 드래그 이동 상대 좌표 계산(0.0~1.0), 단어 카드 포커스 단축키 네비게이션, 프리셋 저장/불러오기, 패널 드래그 기능 터미널 기반 정합성 검증 완료

## [2026-09-17 09:34] 업데이트 이력 (Commit ID: f34717cb1c8cba92ba912208d429e474fd4abc00)
- **수정 내용**:
  - **진행 상황 패널 상단 우측 고정 (스크롤 무관)**: 진행 상황 패널을 스크롤 컨테이너(메인 뷰포트)에서 분리하여 상단 헤더 바로 아래 우측(`top: 12px, right: 24px`)에 항상 안정적으로 고정되도록 개선 (세로 스크롤 및 화면 줌에 전혀 영향받지 않음)
  - **마스크 순번 목록 드래그 앤 드롭 순서 변경**: 왼쪽 패널의 마스크 목록 항목에 6점 드래그 핸들(`⋮⋮`)을 추가하고, 마우스 드래그 앤 드롭으로 마스크 간 순번(`order`)을 실시간 재배치 기능 구현 (캔버스 번호 배지, 단어 카드, 순차 공개 순서 즉시 동기화)
  - **전체화면 모드 시 사이드바 완전 숨김**: 프로젝터/전자칠판 전체화면 투사 시 수업 화면을 가리지 않도록 `:fullscreen` 및 모드 연동 자동 숨김 처리
  - **편집 모드와 전체화면 모드 화면 일치**: 샘플 학습지 캔버스 비율 최적화(1000x720) 및 모드 전환/전체화면 해제/창 크기 조절 시 자동 가로 맞춤 동기화로 3번 문항 잘림 현상 해결
- **검증 결과**:
  - `node --check app.js` JS 구문 분석 통과 (0 error)
  - `verify_ids.js` 정적 분석을 통해 `index.html`과 `app.js` 간 57개 DOM ID 상호 참조 정합성 검증 완료 (누락 ID 0개)
  - 해상도별(1366x768, 1920x1080) 뷰포트 시뮬레이션을 통해 모든 모드에서 1~3번 문항 정상 노출 검증 완료
  - 마스크 순번 재배치 알고리즘(상/하 삽입, order 재할당) 단위 로직 검증 통과

## [2026-09-17 09:42] 업데이트 이력 (Commit ID: 053f436b93d98a4edfa48d38a24e2930de0725f0)
- **수정 내용**:
  - **마스크 순서 변경 시 파일 업로드 오버레이 오작동 차단**: 마스크 목록에서 드래그하여 순서를 바꿀 때 전역 드래그 이벤트로 인해 '여기에 PDF 또는 이미지 파일을 놓으세요' 화면이 뜨던 문제 해결 (`draggedMaskRowId` 플래그 및 `dataTransfer.types.includes('Files')` 검사 적용)
  - **편집 모드 화면 확대 시 문서 하단 스크롤 완벽 지원**: CSS `transform: scale()` 적용 시 뷰포트 스크롤 영역이 확장되지 않아 하단 문제/핵심요약 영역이 잘리던 문제를 해결하기 위해, `setZoom` 시 실시간 확대 비율에 따른 동적 마진(`extraHeight + 100px`) 부여 및 하단 패딩(`padding-bottom: 5rem`) 확보로 문서 맨 아래 끝까지 시원하게 스크롤 가능하도록 개선
- **검증 결과**:
  - `node --check app.js` 자바스크립트 구문 검사 통과 (0 error)
  - `verify_ids.js` 정적 분석을 통해 57개 DOM 엘리먼트 ID 상호 참조 전수 일치 확인 (누락 ID 0개)
  - 마스크 순번 드래그 및 외부 파일 드래그 이벤트 분기 조건 검증 완료
  - 100% ~ 200% 확대 비율별 `canvasWrapper` 하단 마진 및 뷰포트 스크롤 영역 계산 정합성 검증 완료

## [2026-09-17 09:55] 업데이트 이력 (Commit ID: 2eee0f8a6e71e1998bd9e6169a9e5c0f092df538)
- **수정 내용**:
  - **CSS 순환 의존성 제거 및 캔버스 하단 잘림(높이 300px 클램핑) 버그 완벽 해결**: `#renderCanvas`의 `max-width: 100%; height: auto;`로 인해 Flexbox 환경에서 브라우저가 캔버스 높이를 기본 내장 크기(약 300px)로 왜곡 계산하여 Q2 이하(Q3 문항 및 핵심 요약 카드)가 잘리던 문제를 해결하기 위해, `updateCanvasDimensions(w, h)` 메서드를 통해 캔버스 및 래퍼 크기를 항상 명시적인 픽셀 크기(1000x720 또는 원본 해상도)로 100% 동기화
  - **화면 전체 맞춤(fitToPage) 구현 및 기본 적용**: 가로 너비뿐만 아니라 세로 높이까지 동시에 계산하여 가로/세로 중 화면 안에 100% 쏙 들어가는 최적 배율을 계산하는 `fitToPage()`를 신설하고, 모드 전환(편집/전체)/창 리사이즈/전체화면 전환/파일 로드 시 기본 적용하여 상하좌우 잘림 없이 문서 전체가 한눈에 들어오도록 개선
  - **툴바 [화면맞춤] 버튼 신설**: 툴바에 원클릭으로 문서 전체를 한 화면에 쏙 맞출 수 있는 `fitPageBtn` 추가
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - `verify_ids.js` 정적 분석을 통해 `index.html`과 `app.js` 간 58개 DOM 엘리먼트 ID 상호 참조 전수 일치 확인 (누락 ID 0개)
  - 해상도별(작은 창모드 1024x534, 노트북 1366x768, 풀HD 1920x1080) 및 모드별(편집/전체) 뷰포트 시뮬레이션 결과 모든 환경에서 문서 100% 노출(PASS) 검증 완료

## [2026-09-17 10:00] 업데이트 이력 (Commit ID: 33ed6ebdf0be9272af761ba042973b22e0d328eb)
- **수정 내용**:
  - **Flexbox 높이 수축(flex-shrink) 차단 및 캔버스 래퍼 완벽 고정**: Flexbox 부모 환경에서 브라우저 창 높이가 작을 때 `.canvas-wrapper`의 높이가 강제로 수축되어 하단 카드가 잘리던 문제를 `flex-shrink: 0;` 명시를 통해 원천 차단
  - **CSS 스케일 변환에 따른 동적 마진 최적화**: 축소 시(`zoom <= 1.0`) 시각적 높이에 맞추어 음수 마진(`scaledHeight - baseHeight`)을 동적 할당하여 불필요한 스크롤 및 하단 밀림을 제거하고, 확대 시(`zoom > 1.0`)에는 초과 높이만큼 양수 마진을 주어 완벽한 스크롤 보장
  - **화면 전체 맞춤(fitToPage) 0.96 안전 계수 적용**: 뷰포트 크기에 딱 맞추었을 때 브라우저 알림 바나 그림자(box-shadow)로 인해 하단 테두리가 닿던 현상을 방지하기 위해 0.96 안전 계수를 적용하여 상하좌우 20px 이상의 여유 공간을 두고 100% 한눈에 쏙 들어오도록 개선
  - **샘플 학습지 하단 카드 배치 최적화**: 안내 카드 4줄 텍스트 및 테두리 높이를 쾌적하게 정돈하여 카드 바닥부터 캔버스 맨 아래까지 55px의 넉넉한 여백 확보
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - `verify_ids.js` 정적 분석을 통해 58개 DOM 엘리먼트 ID 상호 참조 전수 일치 확인 (누락 ID 0개)
  - 실제 사용자 창 크기(1024x536) 및 다양한 모니터(1366x768, 1920x1080) 시뮬레이션 결과 20px~42px의 안정적인 세로 여유 공간을 두고 문서 100% 완전 노출(PASS) 확인

## [2026-09-17 11:20] 업데이트 이력 (Commit ID: 522cd0035975dd9f4c47c32f5d0f55a4b7c12db0)
- **수정 내용**:
  - **가림판 텍스트 기본값 순서 번호만 표시**: 기본 샘플 학습지 및 신규 생성 가림판의 `text` 기본값을 빈 문자열(`''`)로 설정하여, 사용자가 직접 힌트/단어를 입력하지 않은 기본 상태에서는 불필요한 단어나 정답이 보이지 않고 순서 번호(`1`, `2`, `3`...)만 깔끔하게 표시되도록 개선
  - **가림판 텍스트 항상 왼쪽 정렬(Left-Align) 적용**: `.mask-box`, `.mask-content`, `.mask-text`, `.mask-inline-edit`의 텍스트 정렬을 `justify-content: flex-start; text-align: left;`로 통일하고 여백과 배지 크기를 정돈하여 실제 시험지/학습지의 빈칸 번호 형식으로 자연스럽게 시작되도록 개선
  - **학생용 유인물 인쇄 & PDF 직접 저장 기능 신설**:
    * 상단 툴바에 [유인물 인쇄/PDF] 버튼(`printHandoutBtn`) 및 `Ctrl + P` 단축키 추가
    * `jsPDF` 라이브러리를 탑재하여 원본 문서와 가림판(빈칸 번호 및 테두리)이 합성된 `학생용_유인물_p1.pdf` 파일을 브라우저에서 원클릭으로 직접 다운로드 가능하도록 구현
    * 브라우저 인쇄(`window.print()`) 시 웹사이트 헤더, 메뉴, 사이드바, 플로팅 바를 완전히 숨기고 순수한 학습지만 A4 용지에 출력되는 `@media print` 전용 스타일 구현
    * 수업 중 이미 공개된 가림판이 있더라도 "모든 가림판을 가린 상태로 출력하기" 옵션을 통해 학생 배포용 완전 빈칸 시험지 형태로 자동 인쇄되도록 지원
  - **HTML 배경 흰색 및 메뉴 밝은 계열 테마(Light Mode) 전면 개편**: 학습지 종이 질감에 맞추어 `html`, `body`, `#viewportContainer`의 배경을 순백색(`#ffffff`)으로 전환하고, 상단 헤더, 좌측 사이드바, 버튼, 플로팅 컨트롤러, 모달 등 전체 메뉴를 세련된 밝은 테마(화이트/소프트 슬레이트/로열 블루)로 리디자인
  - **단어 카드 포커스 모드(C) 정답-힌트 위치 교체 및 정답 캔버스 대형화**:
    * 사용자의 피드백을 반영하여 상단에 핵심 정답(원본 가림 영역 크롭 캔버스)을 배치하고, 하단에 보조 교사 힌트 & 메모 영역을 배치
    * 원본 텍스트 높이 대비 약 2.8배~3.5배(최대 가로 960px, 세로 260px)로 정답을 대폭 확대 렌더링하고, 고해상도(Retina 2x) 및 바이큐빅 스무딩을 적용하여 교실 뒤편에서도 선명하게 정답이 보이도록 시각적 품질 극대화
  - **`.agents` 규칙 파일 동기화**: 프로젝트 전용 규칙인 `AGENTS.md`와 `.agents/rules/rules.md`를 100% 일치하도록 동기화 적용
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - `check_ids.js` 정적 분석을 통해 `index.html`과 `app.js` 간 64개 DOM 엘리먼트 ID 상호 참조 전수 일치 확인 (누락 ID 0개)
  - 단어 카드 모달(정답 확대 및 상하 전환), 유인물 PDF 다운로드 및 인쇄 모달, 밝은 테마 적용 정합성 검증 완료

## [2026-09-17 13:15] 업데이트 이력 (Commit ID: c4ffc8158d78ac355f9f4dc000691fabd9dbc97c)
- **수정 내용**:
  - **단축키 `Ctrl + Z` (실행 취소 / 되돌리기) 엔진 신설**:
    * 수업 진행 중 가림판을 잘못 공개하거나, 실수로 가림판을 생성/삭제했을 때 이전 상태로 즉각 복구할 수 있는 통합 액션 히스토리(`actionHistory`) 스택 구축
    * **지원 작업**: 단일 가림판 순차/직접 공개 복구, 신규 생성 가림판 취소, 가림판 삭제 복원, 전체 공개(`A`) 및 다시 가리기(`R`) 되돌리기 완벽 지원
    * **단어 카드 모드 연동**: 단어 카드 포커스(`C`) 상태에서도 `Ctrl + Z` 입력 시 방금 확인한 정답을 다시 가리거나 이전 단어 카드로 자연스럽게 복귀
    * **플로팅 바 연동**: 프레젠테이션 플로팅 컨트롤러의 [이전] 버튼을 `undo()` 엔진에 직접 연결하여 마우스 클릭으로도 손쉽게 되돌리기 가능
  - **도움말 및 UI 단축키 표기 반영**:
    * 좌측 사이드바 하단 '빠른 단축키' 테이블에 `Ctrl + Z (실행 취소/되돌리기)` 추가
    * 프레젠테이션 플로팅 바 [이전] 버튼 툴팁 및 키 힌트를 `[Ctrl+Z]`로 갱신
    * 단축키 안내 모달(`helpModal`) 내 전체 단축키 목록 테이블에 `Ctrl + Z` 항목 명시
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - `check_ids.js` 정적 분석을 통해 `index.html`과 `app.js` 간 64개 DOM 엘리먼트 ID 상호 참조 전수 일치 확인 (누락 ID 0개)
  - 액션 히스토리(`actionHistory`) 스택 동작 및 `prevRevealBtn` 연동 상태 정합성 검증 완료

## [2026-09-17 16:04] 업데이트 이력 (Commit ID: 3e772b81a173ea0935ff442bd5dfa504d4b8733f)
- **수정 내용**:
  - **마스킹 순서 번호 뱃지 외부 돌출형 분리 및 필기 공간 100% 확보**:
    * 기존 가림판 박스 내부(`mask-content`)에 번호 뱃지와 텍스트가 함께 위치하여 학생이 정답을 적을 필기 공간이 부족하던 문제를 해결하기 위해, 순번 뱃지(`mask-badge`)를 가림판 상단 외부 좌측 모서리(`top: -11px; left: 0;`)로 독립 분리
    * 텍스트(힌트)가 없는 일반 빈칸 가림판의 경우 가림판 내부가 완전히 비워진 순수 빈칸 영역으로 유지되어, 유인물 출력 시나 수업 중 학생이 쾌적하게 손글씨 필기를 진행할 수 있도록 개선
  - **외부 돌출 뱃지 시각 디자인 및 테마 최적화**:
    * 흰색 외곽 테두리(`border: 1.5px solid #ffffff`) 및 은은한 그림자(`box-shadow`)를 추가하여 복잡한 학습지 본문 텍스트나 인접 요소와의 시각적 간섭을 차단하고 탁월한 시인성 확보
    * 슬레이트, 포스트잇, 블루, 힌트 블러 테마별로 고대비 뱃지 컬러 팔레트 정돈
    * 캔버스 래퍼(`canvas-wrapper`)의 `overflow`를 `visible`로 정돈하여 문서 외곽 경계선 부근의 마스크 뱃지와 삭제 버튼이 잘리지 않도록 보호
  - **학생용 유인물 인쇄 & PDF 출력 스타일 연동**:
    * `@media print` 시에도 순번 뱃지가 가림판 상단 외부에 높은 대비로 또렷하게 인쇄되고, 가림판 내부는 깔끔한 빈칸 상태로 출력되어 즉시 학생 배포용 유인물로 활용 가능하도록 최적화
- **검증 결과**:
  - `node --check app.js` 자바스크립트 문법 검사 통과 (0 error)
  - 마스킹 상단 외부 뱃지 배치 및 내부 필기 공간 100% 확보 정합성 검증 완료
  - 인쇄 및 PDF 출력 시 순번 뱃지 가시성 및 빈칸 인쇄 렌더링 검증 완료


